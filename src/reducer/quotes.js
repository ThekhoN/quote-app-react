const quotes = (state = [], action) => {
  switch (action.type) {
    case 'ADD_QUOTE':
      return state
    case 'REMOVE_QUOTE':
      return state
    case 'UPDATE_QUOTE':
      return state
    default:
      return state
  }
}

export default quotes
